server {
    listen       80;
    server_name  localhost;

    root   /usr/share/nginx/html;
    index  index.html;

    # A single-page app must fall back to index.html so client-side
    # routes do not 404 on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
