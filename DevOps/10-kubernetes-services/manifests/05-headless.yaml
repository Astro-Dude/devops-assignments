apiVersion: v1
kind: Service
metadata:
  name: web-headless
spec:
  clusterIP: None          # <-- this single line makes it headless
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80
