apiVersion: v1
kind: Service
metadata:
  name: web-nodeport
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - name: http
      port: 8080           # ClusterIP port (a NodePort Service still has one)
      targetPort: 80       # pod port
      nodePort: 30080      # port opened on EVERY node
