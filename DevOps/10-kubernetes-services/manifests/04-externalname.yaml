apiVersion: v1
kind: Service
metadata:
  name: external-db
spec:
  type: ExternalName
  # No selector, no endpoints, no proxying. CoreDNS simply answers queries for
  # external-db.default.svc.cluster.local with a CNAME to the name below.
  externalName: www.google.com
