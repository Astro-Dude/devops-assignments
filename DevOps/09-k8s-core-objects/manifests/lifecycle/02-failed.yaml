apiVersion: v1
kind: Pod
metadata:
  name: phase-failed
spec:
  restartPolicy: Never
  containers:
    - name: worker
      image: busybox:1.36
      command: ["sh", "-c", "echo 'about to fail'; exit 7"]
