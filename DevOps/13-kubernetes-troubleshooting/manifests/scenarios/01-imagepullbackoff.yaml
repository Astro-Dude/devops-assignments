apiVersion: apps/v1
kind: Deployment
metadata: { name: broken-image }
spec:
  replicas: 1
  selector: { matchLabels: { app: broken-image } }
  template:
    metadata: { labels: { app: broken-image } }
    spec:
      containers:
        - name: app
          image: nginx:1.99-does-not-exist     # FAULT: no such tag
